/** @type {import('tailwindcss').Config} */
module.exports = {
  // Content paths are now defined in globals.css using @source directive (Tailwind v4 way)
  // This file is kept minimal for compatibility
};
