[workspace]
members = [
    "core",
    "macros",
]

[profile.release]
panic = "abort"
# Include full DWARF debug info without changing optimization level,
# enabling thru debug resolve to map PCs to source locations.
debug = 2
